#include "includes.h"

#include "field.h"

/// @brief Tries to solve sudoku with specified field.
/// @param f `field` to solve sudoku from.
/// @param row Current row index to start from.
/// @param col Current column index to start from.
/// @return Returns `true` if sudoku solved, otherwise `false`.
bool solve(field *, uint, uint);
